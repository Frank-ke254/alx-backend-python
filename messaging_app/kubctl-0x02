#!/bin/bash
echo "🚀 Deploying Blue-Green setup..."

echo "Applying Blue deployment..."
kubectl apply -f blue_deployment.yaml

echo "Applying Green deployment..."
kubectl apply -f green_deployment.yaml

echo "Applying Service configuration..."
kubectl apply -f kubeservice.yaml

echo "Deployments applied. Checking pod status..."
kubectl get pods -l app=messaging-app

echo "Checking logs for Green deployment..."
GREEN_POD=$(kubectl get pods -l version=green -o jsonpath="{.items[0].metadata.name}")
kubectl logs $GREEN_POD --tail=20

echo "Blue-Green setup complete. To switch traffic, edit kubeservice.yaml and reapply."
